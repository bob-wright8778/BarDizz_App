<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Mic capture (ticket 01) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Keep capture alive while backgrounded/locked (ticket 01) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Required to show/update the persistent foreground-service notification on API 33+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="BarDizz"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Android App Link for the challenge-a-friend email (ticket 05,
                 AC-C5): opens the app for https://bardizz.ca/challenge links.
                 autoVerify requires /.well-known/assetlinks.json hosted at
                 bardizz.ca (manual infra step); until then the OS shows a
                 chooser instead of hard-routing. Uninstalled devices fall
                 through to the browser, which routes on to the store. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="bardizz.ca" android:pathPrefix="/challenge" />
            </intent-filter>
        </activity>

        <!-- flutter_foreground_task's service, declared with the
             "microphone" type Android 14+ requires for a foreground service
             that keeps mic capture running in the background. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- flutter_web_auth_2's redirect-catcher: routes the OAuth
             provider's bardizz://auth/callback redirect back into the app
             (ticket 02, sign-in). -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true"
            android:taskAffinity="">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="bardizz" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
